package io.github.aaronreidsmith.year2025

import io.github.aaronreidsmith.BaseTest

class Day12Test extends BaseTest {
  // The test input is more complex than the actual input, since it requires shapes to actually fit together rather
  // than tile next to each other. Therefore, my solution only works on the actual input
  override val runPart1: Boolean = false

  val suite: Suite = Suite(fileInput, 2)
}
